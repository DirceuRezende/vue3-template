import { defineStore } from 'pinia';

const use{{pascalCase name}}Store = defineStore({
  id: '{{camelCase name}}',
  state: () => ({
    counter: 0
  }),
  getters: {
    doubleCount: (state) => state.counter * 2
  },
  actions: {
    increment() {
      this.counter++;
    }
  }
});

export default use{{pascalCase name}}Store;
