import { render } from '@testing-library/vue';
import {{pascalCase name}} from '.';

describe('<{{pascalCase name}} />', () => {
  it('should render', () => {
    const wrapper = render({{pascalCase name}});

    expect(wrapper).toBeDefined();
  });
});
